<h1>Editing Book</h1>
<% if @book.errors.any? %>
<div>"errors prohibited this book from being saved"</div>
        <ul>
            <% @book.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
            <% end %>
        </ul>
<% end %>
<%= form_for(@book) do |f| %>
    <%= f.label :title%>
    <%= f.text_field :title%>
    <br>
    <%= f.label :body%>
    <%= f.text_area :body%>
    <%= f.submit%>

<td>
<%= link_to "Show", book_path(@book.id) %>
<%= link_to "Back",books_path%>
</td>
<%end%>